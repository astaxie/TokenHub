# 架构决策记录

本目录归档此前保存在本地的 12 篇架构决策，与根目录 `CONTEXT.md` 及 `docs/discussions/` 一起纳入版本管理。

这些记录涵盖不同阶段的设计工作。`accepted` 表示设计决策获接受，不表示当前默认分支已经实现、验证或发布。历史记录中的实现进度和代码位置应按其记录时点理解，并结合目标分支重新核实。

按编号阅读相关决策，并检查后续决策对早期边界的调整。例如 ADR-0012 调整 ADR-0008 的 Provider 统一出口范围。保留历史决定与讨论中的不确定性，不将本次归档当作产品能力交付。
