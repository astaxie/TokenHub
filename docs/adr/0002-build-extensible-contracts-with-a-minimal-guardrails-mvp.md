# Guardrails 先稳定扩展契约，再交付最小能力

TokenHub 第一版 Guardrails 先稳定带有内部配置结构版本的策略、检查点、检测器、风险发现和安全判定契约，但不提供草稿、发布或历史版本等用户功能；管理员保存策略后立即生效，变更复用现有后台审计。产品界面只呈现已经可用的请求侧数据外发防护：规则与敏感数据检测作为正式能力交付，同时用一个默认关闭、默认仅审计但可由管理员改为阻断的 Qwen3Guard 适配器验证模型检测契约；响应、工具调用和其他检测器以后按相同契约加入，不提前成为用户可见配置。
