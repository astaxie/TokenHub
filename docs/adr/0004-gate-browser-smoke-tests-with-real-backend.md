# 使用真实后端与假上游阻断浏览器烟测回归

前端浏览器烟测在 CI 中启动真实 Go 后端、临时 SQLite 和 Next.js，只用本地假上游模拟 Provider 模型发现。Chromium 覆盖登录、Provider 创建和 API Key 发放三条关键流程，并作为独立阻断 job 运行；失败时保留 Playwright trace 与截图。这样既验证浏览器到持久化层的真实集成，又不依赖外网或真实凭据。当前不扩展到多浏览器、全量 E2E 或覆盖率阈值，以控制维护成本和反馈时间。
