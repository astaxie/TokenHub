# TokenHub 领域语言

本文件记录 TokenHub 产品中需要保持一致的业务术语，避免管理界面、API 与文档对同一概念使用不同名称。

> 归档说明：本术语表汇集既有设计工作中的领域定义，不表示默认分支已经实现全部能力。实现状态应结合目标分支代码、测试与相关讨论记录核实。

## 内容安全

**检测器（Detector）**：
识别内容风险并产生风险类别、置信度与原因的能力单元；它不决定最终处置动作。
_避免使用_：过滤器、规则层、模型层

**风险发现（Finding）**：
检测器对一次内容检查产生的风险事实，包含风险类别、原因以及检测器能够可靠提供的原文位置。
_避免使用_：命中结果、模型判断

**安全判定（Decision）**：
安全策略综合一次检查中的风险发现后得出的最终处置结论。
_避免使用_：检测结果、规则结果

**检测降级（Detection Degradation）**：
检测器因超时或不可用而未完成检查，安全策略按照管理员选择的故障行为继续作出判定。
_避免使用_：检测失败、模型异常

**安全策略（Policy）**：
一组已编排的检测器及其判定、升级、降级和处置规则，是管理员配置和应用内容安全能力的主要对象。
_避免使用_：检测器集合、安全配置

**检测项（Detection Item）**：
安全策略中对一个检测器的具体使用方式，包括检查目标和命中后的处置动作；确定性规则和模型检测都以检测项出现。
_避免使用_：防火墙规则、模型规则

**策略绑定（Binding）**：
已发布安全策略与适用业务范围、流量方向及协议之间的关联。
_避免使用_：策略覆盖、策略分配

**检测流水线（Detector Pipeline）**：
安全策略中按判定依赖排列的一组检测器；它允许风险检查逐级升级，但不是管理员自由编排的通用工作流。
_避免使用_：三层架构、检测 DAG、规则链

**检查点（Checkpoint）**：
一次内容检查发生在受保护数据流中的位置；首个可用检查点是内容发送给上游模型之前。
_避免使用_：进站、出站、输入层

**检测器配置（Detector Profile）**：
一个经过验证、可被安全策略引用的检测能力及其适用范围；它描述能力身份，不代表某次策略判定。
_避免使用_：模型配置、检测器实例

**安全基线（Security Floor）**：
由上级业务范围规定、下级范围不能通过普通策略绑定削弱的最低安全要求。
_避免使用_：默认策略、全局覆盖

**安全例外（Security Exception）**：
管理员针对一条安全基线在明确业务范围内设置的临时动作放宽；检测本身仍然执行，并保留管理审计。
_避免使用_：策略覆盖、审批申请

## 数据库演进与版本运维

**应用版本回退（Application Version Rollback）**：
在保留当前 TokenHub 数据库与配置的前提下，激活受支持的较早 Release；只有目标 Release 能安全读写当前数据库状态时才允许。
_避免使用_：系统回滚、数据库回滚、完整回滚

**数据库恢复（Database Recovery）**：
通过备份或快照恢复 TokenHub 数据库的灾难恢复操作，可能丢弃恢复点之后产生的数据；它不属于应用版本回退。
_避免使用_：版本回退、Release 回退

**受支持回退窗口（Supported Rollback Window）**：
能够完整运行于当前 TokenHub 数据库状态、因而允许执行应用版本回退的一组较早 Release；TokenHub 至少保证紧邻的上一个稳定版本，更早版本只有在兼容清单明确允许时才属于该窗口。
_避免使用_：任意降级、全部历史版本

**数据库兼容清单（Database Compatibility Manifest）**：
随 TokenHub Release 发布的数据库兼容声明，描述该 Release 需要达到的数据库状态以及它能够安全读写的状态范围。
_避免使用_：版本映射表、SemVer 兼容推断

**数据库状态版本（Database State Version）**：
TokenHub 数据库已经完成的最新结构迁移所对应的单调编号；它独立于应用版本和数据回填编号。
_避免使用_：Release 版本、应用 SemVer、回填版本

**数据库兼容状态（Database Compatibility State）**：
用于判断一个 TokenHub Release 能否完整运行的数据库状态，由结构迁移版本、必需的阻塞式数据回填和未解决的迁移异常共同确定。
_避免使用_：Schema 版本、表结构版本

**最低直接升级版本（Minimum Direct Upgrade Release）**：
当前 TokenHub Release 明确支持直接识别并升级其数据库状态的最早 Release；更早或未知状态必须先经过受支持的中间版本或数据库恢复流程。
_避免使用_：最旧版本、任意历史版本

**兼容契约测试（Compatibility Contract Suite）**：
验证一个 TokenHub Release 能在指定数据库兼容状态上安全读写并保持关键业务不变量的跨版本测试；只通过启动或健康检查不构成兼容证明。
_避免使用_：启动烟测、Migration 测试

**迁移预检（Migration Preflight）**：
应用数据库变化前对兼容范围、迁移状态和运行条件进行的只读检查；预检通过不代表迁移已经执行成功。
_避免使用_：试运行、迁移成功

**扩展迁移（Expand Migration）**：
在受支持回退窗口内仍允许新旧 Release 共同工作的数据库扩展；它可以随应用启动自动执行，并且至少经历一个稳定 Release 后才能由对应的收缩迁移移除旧结构。
_避免使用_：普通迁移、向前迁移

**收缩迁移（Contract Migration）**：
移除旧数据库结构或结束旧 Release 兼容性的数据库收缩；它只能在兼容窗口关闭后通过显式维护操作执行。
_避免使用_：清理脚本、自动迁移

**阻塞式数据回填（Blocking Data Backfill）**：
当前 Release 进入就绪状态前必须完成的数据转换，因为运行时依赖它建立的数据不变量。
_避免使用_：启动脚本、后台任务

**在线数据回填（Online Data Backfill）**：
TokenHub 保持服务期间可分批执行的数据转换；它必须支持幂等续跑，并由兼容读写路径覆盖尚未转换的数据。应用版本回退会停止执行并保留已提交进度，未来升级时从该进度继续。
_避免使用_：异步脚本、尽力而为迁移

**持续一致性修复（Continuous Invariant Repair）**：
因运行时写入、进程中断或外部状态变化而可能反复出现偏差时，用于持续恢复目标不变量的操作；它不会像一次性数据回填那样永久完成。
_避免使用_：数据回填、数据库迁移

**人工恢复状态（Manual Recovery State）**：
托管升级及其单次自动回切都未能恢复健康服务后进入的受阻状态；TokenHub 停止自动切换版本，等待操作者依据迁移和版本状态完成恢复。
_避免使用_：重试中、自动恢复、降级运行

## Provider 出站访问

**自托管模型上游（Self-hosted Model Upstream）**：
由使用方或其组织部署和管理、通过 API 向 TokenHub 提供模型能力的服务；可以位于本机、容器网络或企业内网，并以 IP 或域名标识。
_避免使用_：本机模型、仅 IP 上游

**Provider 上游请求（Provider Upstream Request）**：
TokenHub 为调用 AI Provider 或其账号资源而发起的出站请求，包括推理、模型发现、凭据刷新和额度查询；身份、通知、Tracing 与版本更新不属于此概念。
_避免使用_：通道请求、Provider 通道、全部出站请求

**出站代理（Outbound Proxy）**：
由平台管理员统一指定、供需要代理的 Provider 上游请求使用的单一转发出口；本机与内网目标默认直连，可显式选择让本地目标也遵循代理策略。已选择代理的请求不会因代理故障回退直连。
_避免使用_：代理出口、通道代理、Provider 专属代理

**Provider 出站模式（Provider Egress Mode）**：
平台管理员对 Provider 上游请求作出的出口选择：继承部署环境代理、明确直连，或使用系统中配置的出站代理；本机与内网目标的默认直连属于目标访问规则，不属于代理故障回退。
_避免使用_：代理优先级、通道出口、代理资源绑定

**代理凭据（Proxy Credential）**：
TokenHub 连接已配置出站代理时使用的 Basic 用户名和密码；密码作为敏感信息加密保存、读取时掩码显示，并且不得进入日志或审计载荷。HTTP 与 HTTPS 是已配置出站代理支持的协议。
_避免使用_：代理 URL 密码、明文代理密码、Provider 凭据

**代理出站故障（Proxy Egress Failure）**：
Provider 尚未收到请求前，在代理选择、连接、认证或等待代理响应阶段发生的平台出口故障；它不会计入 Provider Resource 的失败次数，也不会触发资源冷却。明确的公共代理故障直接结束当前请求，不通过其他 Provider Resource 重复尝试。
_避免使用_：Provider 故障、资源不健康、通道故障

**代理配置传播（Proxy Configuration Propagation）**：
系统设置中的 Provider 出站模式和出站代理配置向 TokenHub 全部实例传播的过程；新请求在最长五秒内使用新配置，已经开始的请求继续使用其启动时的配置。
_避免使用_：立即全局切换、重启生效、实例代理

**代理信任边界（Proxy Trust Boundary）**：
TokenHub 与出站代理各自承担的访问控制责任范围；TokenHub 对上游目标的访问策略同时约束直连与代理请求，配置代理本身不构成对任意目标的访问授权。
_避免使用_：关闭 SSRF、防火墙白名单、代理目标白名单

**最后有效代理配置（Last Known Valid Proxy Configuration）**：
TokenHub 实例最近一次成功加载并验证的 Provider 出站配置。数据库短暂不可读时实例继续使用该配置；配置从未成功加载或敏感字段无法解密时，Provider 上游请求失败且不回退直连。
_避免使用_：默认代理、自动直连、故障回退配置

**代理连通性测试（Proxy Connectivity Test）**：
管理员使用尚未保存或已保存的配置，对一个现有 Provider 地址执行代理连接、认证、隧道和 TLS 握手验证；测试不发送 Provider 凭据或模型请求，测试失败也不阻止配置保存。
_避免使用_：Provider 健康检查、模型测试、代理探活

## Provider 连接器

**Provider 连接器（Provider Connector）**：
向 TokenHub 提供可治理的上游模型访问能力的独立边界；上游账号的取得、认证续期和账号池策略属于连接器自身，而不是 TokenHub。
_避免使用_：Token 来源、账号通道、订阅账号适配器

**托管连接器（Managed Connector）**：
由 TokenHub 负责安装、版本和运行生命周期的 Provider 连接器；连接器仍然拥有其上游账号与认证生命周期。
_避免使用_：内置账号、TokenHub 账号服务、进程内适配器

**外部连接器（External Connector）**：
由操作者独立部署和运行、TokenHub 仅建立受控连接的 Provider 连接器。
_避免使用_：远程账号池、外部 Token、非托管账号

**连接器插件（Connector Plugin）**：
可安装为托管连接器的分发单元；“插件”描述安装体验，不表示第三方代码加载进 TokenHub 进程，也不表示向 TokenHub 交付上游账号凭据。
_避免使用_：Token 插件、账号插件、进程内插件

## 用量与计费

**上游成本（Provider Cost）**：
TokenHub 因调用供应商而承担的费用，与向租户收取的费用是不同的业务概念。
_避免使用_：租户费用、统一费用、模型价格

**租户费用（Tenant Charge）**：
按 TokenHub 与租户约定的收费规则计算、归属于该租户的费用；上游成本变化不等于租户收费规则变化。
_避免使用_：上游成本、供应商账单、统一费用

**上游缓存读取（Provider Cache Read）**：
供应商报告为缓存命中的输入用量，属于本次上游模型调用的输入组成部分。
_避免使用_：网关缓存命中、免费输入、响应复用

**上游缓存写入（Provider Cache Write）**：
供应商独立计量的缓存创建用量，可能按缓存有效期区分收费项。
_避免使用_：普通输入、网关缓存存储、统一缓存费

**网关响应缓存（Gateway Response Cache）**：
TokenHub 复用已有响应、从而免去本次上游模型调用的机制。
_避免使用_：上游缓存读取、提示词缓存、供应商缓存命中

**租户价目（Tenant Rate Card）**：
TokenHub 对租户各项用量约定的收费价格，可包含适用时段；它独立于供应商向 TokenHub 收取的价格。
_避免使用_：供应商报价、成本价、模型目录参考价

**价格版本（Price Version）**：
一组具有明确生效时间、发布后保留不变的计价规则，是解释相应用量费用的依据。
_避免使用_：最新价格、模型版本、临时价格

**上游原币金额（Provider Original-Currency Amount）**：
按供应商价目所用币种计算的上游费用金额；它与为统一核算而产生的折算金额分别表达。
_避免使用_：美元成本、折算费用、无币种金额

**费用调整（Billing Adjustment）**：
对既有费用作出的可追溯增减，保留原费用及其依据。
_避免使用_：覆盖账单、修改历史价格、重新扣费

**预算预留（Budget Reservation）**：
请求执行前为其预计费用占用的可用预算，最终按实际费用结算；它不是充值余额扣款，也不是已确认费用。
_避免使用_：钱包冻结、实际消费、最终扣款

**待核实用量（Unverified Usage）**：
因供应商计量证据不足而不能确定全部计费分项的用量，可附带明确标识的估算。
_避免使用_：零用量、免费请求、实测用量

**上游调用尝试（Upstream Attempt）**：
TokenHub 为完成一次调用向供应商发起的一次执行尝试；重试或切换供应商会产生新的尝试。
_避免使用_：租户调用、最终响应、唯一费用

**计费时段（Billing Period）**：
价目规则按时区、星期和日内时间界定的价格适用区间，例如高峰和空闲时段。
_避免使用_：实时负载、账单周期、预算周期

**汇率版本（Exchange Rate Version）**：
具有明确生效时间、用于将原币金额折算为核算币种的已发布汇率依据。
_避免使用_：当前汇率、供应商价格、无依据折算

**预算周期（Budget Period）**：
按 UTC 日或月界定的预算归集区间；请求费用与预算预留归属于其准入时确定的周期，后续调整仍关联该周期。
_避免使用_：计费时段、峰谷窗口、结算日期

**部分交付（Partial Delivery）**：
一次调用在终止前已向调用方交付部分模型输出；其租户费用以可确认的交付用量为依据，证据不足的部分保持待核实。
_避免使用_：免费失败、完整成功、完整上游输出

**历史依据不足（Legacy Evidence Incomplete）**：
历史费用已留存，但缺少足以复原当时全部价格、分项用量或汇率的证据。
_避免使用_：零费用、已核对账单、可精确重算

**调用归属用户（Attributed User）**：
一次业务调用在用量及费用归集中归属的用户；该身份与供应商执行请求时所用的上游资源账号独立。
_避免使用_：上游账号、供应商资源、Provider 用户

**上游资源账号（Provider Resource Account）**：
供应商调用所使用的资源账号身份；它不表示持有 TokenHub API Key 或承担业务费用的用户。
_避免使用_：API Key 所属用户、调用归属用户、项目负责人
