@AGENTS.md

## Agent skills

### Issue tracker

使用 astaxie/TokenHub 的 GitHub Issues，操作规范见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认 triage 状态标签，映射见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局：根目录 `CONTEXT.md` 和 `docs/adr/`。
阅读规则见 `docs/agents/domain.md`。
